export const checkUserSelector = (state) => state.user;
export const checkQuantitySelector = (state) => state.quantityCart;
export const checkIsAddToCartSelector = (state) => state.isAddToCart;
export const checkTotalProductsSelector = (state) => state.totalProducts;
export const checkProductFavoriteSelector = (state) => state.productFavorite;
